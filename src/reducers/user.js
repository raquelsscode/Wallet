// Esse reducer será responsável por tratar as informações da pessoa usuária

const userobj = {
  user: {
    email: '',
  },
};

const user = (state = userobj, action) => {
  switch (action.type) {
  default: return state;
  }
};

export default user;
